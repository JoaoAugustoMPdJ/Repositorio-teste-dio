apiVersion: v1
kind: Service
metadata:
  name: app-gcp-kubernetes-service
spec:
  type: LoadBalancer
  selector:
    app: app-gcp-kubernetes
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080